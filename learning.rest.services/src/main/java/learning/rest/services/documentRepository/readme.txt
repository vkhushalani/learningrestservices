###### Start ReadMe ########

1. To connect/create REP_NAME repository 
2. To create a folder and document inside the root folder
3. Need to include the ref resource in the web.xml
	<resource-ref>
	   <res-ref-name>EcmService</res-ref-name>
	   <res-type>com.sap.ecm.api.EcmService</res-type>
	</resource-ref>
###### End ReadMe #######